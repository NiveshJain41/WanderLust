<% layout("/layouts/boilerplate.ejs") %>
<body>
    <h3>All listings</h3>
    <!-- 20. CREATING  -->
    <!-- <form method="GET" action="/listings/new">
        <button>Create New </button>
    </form> -->
    <!-- the above form is commented because add listing form is now in navbar -->

    <!-- adding card styling -->
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 g-3">
        <% for (let listing of allListings) { %>
            <a href="/listings/<%=listing._id%>" class="listing-link">
                <div class="col">
                    <div class="card h-100">
                      <img src="<%= listing.image %>" class="card-img-top" alt="listing item" style="height: 20rem;" >
                      <div class="card-body">
                        <p class="card-text">
                          <b><%= listing.title %></b> <br>
                          <% if (listing.price !== undefined && listing.price !== null) { %>
                            &#8377;<%= listing.price.toLocaleString("en-IN") %>/night
                          <% } else { %>
                            <span class="text-muted">Price not available</span>
                          <% } %>
                          
                        </p>
                      </div>
                    </div>
                  </div>
            </a>
          
        <% } %>
      </div>
      
    

    <!--16. Showing all listing and sending to "listing/id" -->
    <!-- <ul>
        <% for(let listing of allListings){ %>
            <li>
                <a href="/listings/<%=listing._id%>"><%=listing.title%></a>
            </li>
        <% } %>
    </ul> -->
    <!-- This section is commented because it is later implemented using cards -->
</body>
